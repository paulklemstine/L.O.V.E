Refactored the main UI loop to reuse a single 'rich.Console' instance via a helper class 'OffscreenRenderer'. This prevents the performance overhead of repeatedly instantiating Console objects (which perform terminal capability checks) for every frame of animation or log message.
